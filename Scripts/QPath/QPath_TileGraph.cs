using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace QPath {
	public class QPath_TileGraph {

		// the graph's job is to keep a list of all neighbors leaving a tile
		Dictionary<IQPathTile, IQPathTile[]> neighbors;

		public QPath_TileGraph( IQPathWorld world ) {
			//
					
		}
	}
}